import { Inter, Outfit } from 'next/font/google'

export const INTER_FONT = Inter({
    subsets: ['latin'],
    display: 'swap',
    variable: '--font-inter',
})

export const OUTFIT_FONT = Outfit({
    subsets: ['latin'],
    display: 'swap',
    variable: '--font-outfit',
})
